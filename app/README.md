# app

The browser playground. Vite + React + TypeScript; `src/laya` is the TypeScript port of
the Python model API in `../export`.

See the [root README](../README.md) for what it is, how to run it, and how it deploys.
